class message {
    // Success
    static USER_CREATED = `User signup successfully`;


    //Error
    static USER_EXIST = `User already exist`

    // Required
    // static USER_REQUIRED = `# is required`
    static FIELD_REQUIRED = `# is required`
    static VALID_INPUT = `Please provide valid #`
}

export { message }